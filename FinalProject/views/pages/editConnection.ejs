<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Campus Connections - Edit Connection</title>

    <link rel='stylesheet' href='/public/css/global.css'>
    <link rel="stylesheet" href="/public/css/newConnection.css">
</head>
<body>
    <%- include('../components/header') %>

    <main>
        <!--topic, title, host, details, where, when, start, end, image url-->
        <form action="/connection/edit/<%= connection._id %>" method="post">
            <h2>Edit Connection</h2>

            <label>Title</label>
            <input type="text" placeholder="title" name="title" value="<%= connection.title %>">

            <label>Topic</label>
            <input type="text" placeholder="topic" name="topic" value="<%= connection.topic %>">

            <label>Details</label>
            <textarea name="details"><%= connection.details %></textarea>

            <label>Location</label>
            <input type="text" placeholder="location" name="location" value="<%= connection.location %>">

            <label>Date</label>
            <input type="date" name="date" value="<%= connection.date %>">

            <label>Start</label>
            <input type="time" name="start_time" value="<%= connection.start_time %>">

            <label>End</label>
            <input type="time" name="end_time" value="<%= connection.end_time %>">

            <label>Image URL</label>
            <input type="text" placeholder="image url" name="img" value="<%= connection.img %>">

            <div class="messages">
                <% if (locals.errorMessages.length > 0) { %> 
                    <% locals.errorMessages.forEach(err => { %> 
                        <p class="error"><%= err %></p>
                    <% }) %>
                <% } %>
            </div>
            
            <input type="submit" class="button">
        </form>
    </main>

    <%- include('../components/footer') %>
</body>
</html>